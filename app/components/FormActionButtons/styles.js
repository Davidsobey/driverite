import styled from 'styled-components';

const FormActionButtonsWrapper = styled.div`
  margin-top: 30px;
  display: flex;
  justify-content: space-between;
`;

export default FormActionButtonsWrapper;
