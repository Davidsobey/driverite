/**
 * Direct selector to the rotation state domain
 */
// import { createSelector } from 'reselect';

// export const selectAllRotations = (state) => {
//   try {
//     return;
//   } catch (e) {
//     return '';
//   }
// };

// const makeSelectUsername = () => createSelector(
//   selectHome,
//   (homeState) => homeState.get('username')
// );
